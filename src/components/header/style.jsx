import styled from 'styled-components'

const header = styled.div`
  padding: 1em 2em;
  background-color: rgb(197, 235, 216);
  display: flex;
  justify-content: flex-end;
`
const s = {
  header,
}

export default s
